/*
 * {{ layout.name }}: {{layout.docstring}} ({{layout.width}} bits)
 */
{{ layout.name }} ({{ layout.docstring }}) {{ layout.start }} - {{ layout.width }}

{% case layout.type %}
{%  when "layout" %}
{%   for dd in layout.children %}
  cc
{%    include "c_layout.ede" with layout = dd.value %}
{%   endfor %}
{%  when "layout_bitmaps" %}
{#
{%   for d in layout.children %}
{%    include "c_layout_bitmaps.ede" with layout = d.value %}
{%   endfor %}
#}
yy
{% endcase %}
